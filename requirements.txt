anthropic>=0.40.0
requests>=2.31.0
flask>=3.0.0

# Local development — loads .env file automatically:
python-dotenv>=1.0.0

# Twitter/X posting (OAuth 1.0a required for write access):
requests-oauthlib>=1.3.0

# Google Indexing API — required if google_indexing.enabled = true:
google-auth>=2.0.0

# Uncomment if using python-telegram-bot instead of raw requests:
# python-telegram-bot>=20.0
